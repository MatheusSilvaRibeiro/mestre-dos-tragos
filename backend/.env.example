DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/mestre_dos_tragos?schema=public"
JWT_SECRET=seu_secret_aqui
PORT=3333

# Opcionais — limites de requisicao (ver src/middlewares/rateLimit.ts).
# Sem definir, usa os valores seguros de producao (100 e 5). Suba
# temporariamente so para rodar a suite E2E completa, depois volte ao padrao.
# RATE_LIMIT_MAX=300
# LOGIN_RATE_LIMIT_MAX=20